apply plugin: 'java'
apply plugin: 'kotlin'

dependencies {
    compile project(':core')
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
    compile "com.aparapi:aparapi:1.8.0"
    testCompile 'junit:junit:4.12'
}
